{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Campaign list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Checker</th>
                <th>Suppliername</th>
                <th>Status</th>
                <th>Expires_at</th>
                <th>Created_at</th>
                <th>Updated_at</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('supplier_campaign_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.checker }}</td>
                <td>{{ entity.supplierName }}</td>
                <td>{{ entity.status }}</td>
                <td>{% if entity.expiresat %}{{ entity.expiresat|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.createdat %}{{ entity.createdat|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updatedat %}{{ entity.updatedat|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('supplier_campaign_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('supplier_campaign_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('supplier_campaign_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
